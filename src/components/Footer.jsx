const Footer = () => (
  <footer className="footer">
    <div className="container" style={{ padding: '16px 24px' }}>
      <small>© {new Date().getFullYear()} Jorge — Hecho con React.</small>
    </div>
  </footer>
);

export default Footer;
